# spring-authorization-server
production-ready baseline for a Spring Authorization Server (SAS) backed by PostgreSQL. It issues JWT access tokens, supports PKCE, client_credentials, refresh tokens, and exposes the OIDC endpoints
